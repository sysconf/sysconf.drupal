#
# Example of configuration of a state branch with the "git-remote" driver
#
[state]
        driver = git-remote

[git-remote]
        url = user@host:server/repo.git
        url = /path/to/bare/repository
        url = /path/to/repository

        remoteRefs = refs/*
        localRefs = *
